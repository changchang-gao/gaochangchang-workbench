# 定制工作台 · 产品需求文档（PRD）

> 文档整理自当前代码实现（`/Users/zhihu/gaochangchang-workbench/index.html`）。
> 文档名中的「定制工作台」为产品当前名称（曾用名「高畅畅专属工作台」「畅畅专属工作台」）。

---

## 1. 产品概述

| 项 | 说明 |
|----|------|
| 产品定位 | 面向家庭/个人的多功能生活管理单页应用（SPA），主打温馨、记录、复利成长 |
| 目标用户 | 家庭成员（如「畅畅」「七宝」），支持家人协作 |
| 技术架构 | 纯静态单文件 `index.html` + Tailwind CSS（CDN）+ Supabase（BaaS：PostgreSQL 存储 + Auth 鉴权 + Realtime 实时订阅） |
| 部署 | Cloudflare Pages（当前域名 `gaochangchang-workbench.pages.dev`） |
| 设计风格 | 温馨粉色系（`#fff0f3`/`#ff5c7f` 等）、圆角卡片、轻量动效（fade-in） |
| 响应式 | 桌面端左侧完整侧边栏（含文字），移动端侧边栏收起为纯图标（`w-14` vs `md:w-56`） |

---

## 2. 用户与权限体系

1. **注册/登录**：邮箱 + 密码（Supabase Auth），登录页含「登录 / 注册」切换，注册需填昵称。
2. **工作空间（Workspace）**：
   - 用户首次登录自动创建一个工作空间（默认命名：`昵称 + 的工作空间`）。
   - 数据通过 `workspace_id` 隔离，仅本工作空间成员可见。
3. **成员角色**：`owner`（拥有者）/ `editor`（编辑者）；权限由 Supabase 行级安全（RLS）策略控制。
4. **邀请与协作**：
   - 每个工作空间有 8 位邀请码（`invite_code`）与邀请链接（`?invite=CODE`）。
   - 支持复制邀请码 / 邀请链接，分享给家人注册后加入。
   - 支持输入邀请码主动加入他人工作空间（角色为 `editor`）。
5. **实时同步**：通过 Supabase Realtime 监听 `memos / daily_templates / daily_task_instances / swimming_records / plans / schedules / plan_subtasks` 表变化，自动刷新当前视图。
6. **退出登录**：清除会话与实时频道，回到登录页。

---

## 3. 功能模块

左侧导航共 9 个模块：**首页、备忘录、我的日程、七宝每日任务、游泳练习、未来计划、收藏、小本本、设置**。

### 3.1 首页（工作台）
- 标题区：标题「工作台」+ 副标题「温柔坚持，复利成长」+ 顶部日期（农历/公历中文格式）。
- 概览统计卡片：
  - 备忘录完成度
  - 未来计划进度（含子任务完成比例）
  - **累计游泳时长**：同时显示「X 分钟（Y 小时）」，保留原分钟、额外显示小时。
- 快捷入口卡片：备忘录 / 我的日程 / 游泳练习 / 未来计划 / 收藏 / 小本本（点击跳转对应模块）。
- **我的日程卡片**：副标题动态显示**最近一次未来日程**（标题·日期），无未来日程时回退默认文案「生日、纪念日、重要日子」。

### 3.2 备忘录
- 记录日常备忘、零散小事。
- 功能：新增、编辑、删除、完成勾选（打勾）。
- 数据表：`memos`（id, workspace_id, title/content, completed, created_by, created_at）。

### 3.3 我的日程
- 记录生日、纪念日、重要日子等。
- 功能：按日期（升序）展示；新增、编辑、删除日程（含标题、日期、类型等）。
- 目标：「让美好不被遗忘」。
- 数据表：`schedules`（id, workspace_id, title, date, type, created_by, created_at）。

### 3.4 七宝每日任务
- 设计：模板（`daily_templates`）+ 每日实例（`daily_task_instances`）。
- 选择日期后**自动从该日生成每日任务**，完成即打卡。
- **模板管理**：支持新增、编辑、删除、排序（sort_order）。
- 首次创建工作空间时预置默认模板：晨起喝水、读绘本 20 分钟、户外运动 30 分钟、整理自己的玩具。
- 数据表：`daily_templates`、`daily_task_instances`。

### 3.5 游泳练习
- 记录每次游泳的日期、时长（分钟）、备注。
- **累计统计**：顶部卡片左侧显示「累计游泳时长 X 分钟」，右侧独立显示「累计小时 Y 小时」（分钟与小时同时保留）。
- 功能：新增（日期 + 时长 + 备注）、删除。
- 数据表：`swimming_records`（id, workspace_id, date, duration, note, created_by, created_at）。

### 3.6 未来计划
- 把大目标拆成子任务，进度一点点推进。
- 功能：新增计划、删除计划；计划下增删子任务（`plan_subtasks`）、勾选子任务完成。
- 数据表：`plans`、`plan_subtasks`（含 sort_order、completed）。

### 3.7 收藏 ⭐
- 按 5 个固定分类组织：影视、书籍、儿童读物、儿童影视、其他（category key 如 `kids_book`）。
- 功能：
  - 每个分类下条目的新增、编辑、删除。
  - 顶部搜索框（按名称/备注/标签模糊搜索，🔍 图标不遮挡输入）。
  - **每个分类头部支持「收起 ▼ / 展开 ▶」**，各分类折叠状态独立记忆。
  - 桌面端分类以两列网格展示（`md:grid-cols-2`），移动端单列。
- 数据表：`collections`（id, workspace_id, category, title, note, tags, created_by, created_at）。

### 3.8 小本本 📓（原「记仇本」）
- 文本型记录模块：每条含标题 + 正文（多行）。
- 功能：新建文本、保存、编辑（复用同一弹窗预填）、删除（二次确认）。
- 列表按时间倒序，正文保留换行。
- 数据表：`grudges`（id, workspace_id, title, content, created_by, created_at）。
- 首次访问若无表，页面提示建表 SQL（独立文件 `grudges.sql`）。

### 3.9 设置 ⚙️
- 工作空间信息（名称、当前用户角色）。
- 成员列表（人数、角色标识）。
- 邀请家人：展示邀请码、复制邀请码 / 复制邀请链接。
- 加入他人工作空间：输入 8 位邀请码加入。
- 退出登录。

---

## 4. 数据模型（Supabase 表概览）

| 表名 | 主要字段 | 用途 |
|------|----------|------|
| `workspaces` | id, name, invite_code | 工作空间 |
| `workspace_members` | workspace_id, user_id, role | 成员关系与角色 |
| `memos` | workspace_id, title/content, completed | 备忘录 |
| `schedules` | workspace_id, title, date, type | 日程/纪念日 |
| `daily_templates` | workspace_id, title, sort_order | 每日任务模板 |
| `daily_task_instances` | workspace_id, date, title, completed | 每日任务实例 |
| `swimming_records` | workspace_id, date, duration, note | 游泳记录 |
| `plans` | workspace_id, title | 未来计划 |
| `plan_subtasks` | plan_id, title, completed, sort_order | 计划子任务 |
| `collections` | workspace_id, category, title, note, tags | 收藏 |
| `grudges` | workspace_id, title, content | 小本本 |

> 所有业务表均带 `workspace_id` 字段并开启 RLS，确保数据按工作空间隔离。

---

## 5. 通用交互与体验规范

- **响应式**：移动端侧边栏仅显示图标，桌面端展开文字（`md:` 断点）。
- **Toast 提示**：操作成功/失败/信息反馈。
- **删除二次确认**：关键删除（如小本本条目、计划）需确认。
- **弹窗表单**：新增/编辑统一用模态弹窗。
- **空状态提示**：无数据时显示引导文案。
- **建表引导**：`collections` / `grudges` 等表首次访问若缺失，页面给出建表 SQL 提示（`grudges.sql` 已独立提供）。
- **本地运行注意**：因 Supabase 跨域限制，需通过本地 HTTP 服务器（如 `http://localhost:8899`）打开，不能直接 `file://` 双击。

---

## 6. 历史需求优化记录（迭代摘要）

| 阶段 | 需求点 | 说明 |
|------|--------|------|
| 初始修复 | 收藏模块语法错误修复 | JS 比较运算符/HTML 标签被剥离，整体重写并修复路由 |
| UI 修复 | 搜索框图标重叠 | 加大内边距 + `pointer-events-none` |
| UI 优化 | 分类卡片布局 | 竖排 → 桌面两列（中间尝试过 5 列后撤销） |
| 数据 | 添加 9 本儿童读物 | 写入「儿童读物」分类（kids_book） |
| 功能 | 分类收起/展开 | 每个分类头部折叠按钮，状态独立记忆 |
| 新模块 | 小本本（记仇本） | 左侧入口 + 右侧新建/保存/编辑/删除 |
| 配套 | grudges 建表 SQL | 独立 `grudges.sql` |
| 改名 | 记仇本 → 小本本 | 仅改显示名，内部标识不变 |
| 改名 | 高畅畅专属工作台 → 定制工作台 | title + 登录页 + 首页标题 |
| 改名 | 畅畅专属工作台 → 工作台 | 首页标题 |
| 视觉 | 头像更换 | 文字「盼」→ 柴犬戴眼镜图（base64 内嵌） |
| 统计 | 游泳/首页累计时长 | 增加小时显示，保留原分钟显示 |
| 首页 | 我的日程副标题 | 动态显示最近一次未来日程 |

---

## 7. 待明确 / 后续可优化项

1. **域名变更**：计划将 `gaochangchang-workbench.pages.dev` 改为 `wyj-workbench.pages.dev`；Cloudflare「重命名项目」仅改显示名、不改站点地址，需通过「新建 Pages 项目 + 关联同一仓库 + 删旧项目」实现（待在 Cloudflare 控制台操作）。
2. **表初始化**：`collections` / `grudges` 等表若未在 Supabase 建好，首次访问会提示建表 SQL，建议统一整理全部建表脚本。
3. **可扩展性**：目前分类（收藏）、模块均为硬编码，后续可考虑配置化。
4. **移动端体验**：部分弹窗/表格在移动端可进一步细化。

---

*整理日期：2026-08-20 · 基于代码实现版本（commit `ea10355` 及之前）。*
